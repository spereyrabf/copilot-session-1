services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: fastapi-jwt-backend
    environment:
      JWT_SECRET_KEY: ${JWT_SECRET_KEY}
    ports:
      - "8000:8000"
